name: main_project
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

# resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  design_system:
    path: ../design_system
  login_module:
    path: ../login_module
  profile_module:
    path: ../profile_module

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
